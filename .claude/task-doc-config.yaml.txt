## 任务：输出主机文档 + 更新 config.yaml

### 背景
28 台 ESXi 主机连通性测试已跑完。需要做两件事：

### 1. 输出主机清单文档
请读取最新一次 `test_vsphere_connectivity.py` 的完整输出（终端缓冲区里应该有逐台结果），生成文档 `docs/hosts-inventory.md`，包含：

- 一张完整表格：IP | 版本(完整版本号) | 密码 | VM数 | 状态
- 汇总统计：按版本分组（ESXi 5.1.0 / 5.5.0 / 8.0.0）、总 VM 数、总主机数
- 对于连接失败的（172.16.0.251），标注"密码错误"

### 2. 更新 config.yaml
在 `/home/lan/esxi-mcp/config.yaml` 中，把所有连接成功的主机都写入 hosts 字典。

密码分组规则：
- 密码 `zkgillion368` 的主机 → password_env: "VSPHERE_PASSWORD_ESXI51"
- 密码 `zkGillion@368` 的主机 → password_env: "VSPHERE_PASSWORD_ESXI80"
- 172.16.0.251：用户说密码是 `Gillion@168`，设为 password_env: "VSPHERE_PASSWORD_ESXI251"

所有主机统一配置：
- username: "root"
- port: 443
- insecure_ssl: true

每台主机的 key 用 IP 最后一段命名，如：
```yaml
esxi-192:
  host: "172.16.0.192"
  port: 443
  username: "root"
  password_env: "VSPHERE_PASSWORD_ESXI51"
  insecure_ssl: true
```

config.yaml.template 已存在（模板文件），config.yaml 已在 .gitignore 中排除。

### 完成后
运行 `python3 -m pytest tests/ -v` 确认 32/32 测试通过。
